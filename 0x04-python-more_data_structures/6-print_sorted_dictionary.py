#!/usr/bin/python3
def print_sorted_dictionary(a_dictionary):
    if dict is not None:
        for keys in sorted(list(a_dictionary.keys())):
            print("{} : {}".format(keys, a_dictionary.get(keys)))
